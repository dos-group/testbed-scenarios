
collector:
  extra_args: -exclude "net-io/nic/" -exclude "ovsdb/" -files-check-output 2s -files-buf 64 -s "tags(tags={ layer='physical', node='{{ ansible_hostname }}', component='{{ ansible_hostname }}' })"

apt_list:
  - htop
  - bmon

docker_events_exposer:
  local_server_name: "{{ ansible_fqdn }}" # For Hypervisors, the hostname must be given from the outside
