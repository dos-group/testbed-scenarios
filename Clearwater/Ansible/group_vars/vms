
collector:
    extra_args: -exclude "net-io/nic/" -files-check-output 2s -files-buf 64 -s "tags(layer=virtual, vm='{{ libvirt_id }}', node='{{ hypervisor }}', component='{{ hypervisor }}-{{ libvirt_id }}')"

apt_list:
  - apt-transport-https
  - ca-certificates
  - curl
  - software-properties-common
  - htop
  - bmon
  - jq
  - iotop
