#!/usr/bin/env ansible-playbook

- include: reset-collector-injector.yml

- name: Delete collector data
  hosts: all:!client
  tasks:
    - file: path={{ collector.data_path }} state=absent
      become: true
    - file: path={{ collector.data_path }} state=directory
      become: true
